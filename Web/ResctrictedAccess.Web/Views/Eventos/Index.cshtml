@model ResctrictedAccess.Web.Models.Eventos.Index.ViewModel
<h2>Eventos</h2>
@Html.ActionLink("Novo", "Create", "Eventos")
<table class="table">
    <thead>
        <tr>
            <th>
                Nome
            </th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Eventos)
        {
            <tr>
                <td>
                    @item.Nome
                </td>
                <td>
                    @Html.ActionLink("Editar", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Detalhes", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Excluir", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>
